@using System.Security.Principal

    <ul class="navbar-nav">
        @if (User.Identity.IsAuthenticated)
        {

            <li class="nav-item dropdown">
                <a class="nav-link" data-toggle="dropdown" href="#">
                    <i class="far fa-bell"></i>
                    <span class="badge badge-warning navbar-badge"></span>
                </a>
                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                    
                    <a href="#" class="dropdown-item">
                        <i class="fas fa-file mr-2"></i>
                        Sample Notifications here
                        <span class="float-right text-muted text-info">2</span>
                    </a>
                    <div class="dropdown-divider"></div>
                  
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link text-info" data-toggle="dropdown" href="#">
                    @User.Identity.Name
                </a>
                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                    <a class="nav-link text-info" href="#">Account Settings</a>
                    <div class="dropdown-divider"></div>
                    <a class="nav-link text-danger" asp-area="AzureAD" asp-controller="Account" asp-action="SignOut">Sign out</a>
                    <div class="dropdown-divider"></div>
                </div>

            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-info" asp-area="AzureAD" asp-controller="Account" asp-action="SignIn">Sign in</a>
            </li>
        }
    </ul>


